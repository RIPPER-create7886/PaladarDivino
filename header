<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurante Los Huastecos</title>
    
    <link rel="icon" href="logo.png" type="image/png">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
    
    <style>
        /* === 1. COLORES DE LA MARCA === */
        :root {
            --color-terracota: #c04e01; /* Naranja Ladrillo */
            --color-maiz: #f39c12;      /* Amarillo */
            --color-crema: #fdfbf0;     /* Fondo suave */
            --color-verde: #2d5a27;     /* Verde naturaleza */
        }

        body { 
            padding-top: 100px; /* Espacio para que no se tape el menú */
            font-family: 'Segoe UI', sans-serif;
            background-color: var(--color-crema) !important; /* Fuerza el color de fondo crema */
        }

        /* === 2. BARRA DE NAVEGACIÓN (HEADER) === */
        .navbar-custom {
            background-color: var(--color-terracota) !important; /* FONDO NARANJA OBLIGATORIO */
            border-bottom: 5px solid var(--color-maiz);
            box-shadow: 0 4px 10px rgba(0,0,0,0.3);
            min-height: 80px;
        }

        .navbar-brand { 
            font-weight: 800; 
            color: #ffffff !important; 
            text-transform: uppercase;
            font-size: 1.5rem;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5);
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .nav-link {
            color: rgba(255,255,255,0.95) !important; /* Texto blanco */
            font-weight: 600;
            font-size: 1.1rem;
            margin-left: 15px;
        }

        .nav-link:hover {
            color: var(--color-maiz) !important; /* Amarillo al pasar el mouse */
        }

        /* Estilo para el Logo */
        .logo-oficial {
            height: 60px;
            width: auto;
            border-radius: 50%;
            border: 2px solid white;
            background: white;
            transition: transform 0.3s;
        }
        .navbar-brand:hover .logo-oficial { transform: rotate(-10deg) scale(1.1); }

        /* === 3. ESTILOS DE TARJETAS (MENÚ) === */
        /* Esto arregla el diseño en pedidos.php */
        .card {
            border: none !important;
            border-radius: 15px !important;
            background-color: white !important;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            overflow: hidden;
        }

        /* EFECTO LEVANTAR (HOVER) */
        .card:hover {
            transform: translateY(-10px) !important;
            box-shadow: 0 20px 40px rgba(0,0,0,0.2) !important;
            cursor: pointer;
            z-index: 99;
        }

        .card-img-top {
            height: 220px;
            width: 100%;
            object-fit: cover;
            transition: transform 0.5s;
        }
        .card:hover .card-img-top { transform: scale(1.05); }

        /* === 4. TIPOGRAFÍA DEL MENÚ === */
        .titulo-plato { color: var(--color-terracota); font-weight: 800; font-size: 1.25rem; }
        .precio { color: var(--color-verde); font-size: 1.4rem; font-weight: bold; }
        
        /* Títulos de secciones */
        .categoria-header {
            background-color: #e9ecef;
            padding: 10px 20px;
            border-left: 8px solid var(--color-terracota);
            margin: 30px 0 20px 0;
            font-weight: bold;
            color: #333;
            text-transform: uppercase;
            border-radius: 0 10px 10px 0;
        }

        /* === 5. PIE DE PÁGINA === */
        .footer { 
            background: #222; 
            color: white; 
            padding: 40px 0; 
            margin-top: 60px; 
            text-align: center;
            border-top: 6px solid var(--color-terracota);
        }
    </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-custom fixed-top">
  <div class="container">
    
    <a class="navbar-brand" href="index.php">
        <img src="logo.png" 
             alt="Logo" 
             class="logo-oficial"
             onerror="this.src='https://cdn-icons-png.flaticon.com/512/4359/4359642.png'"> 
        LOS HUASTECOS
    </a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon" style="filter: invert(1);"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item"><a class="nav-link" href="index.php">Inicio</a></li>
        <li class="nav-item"><a class="nav-link" href="pedidos.php">Menú Huasteco</a></li>
        <li class="nav-item"><a class="nav-link" href="contacto.php">Contacto</a></li>
      </ul>
    </div>
  </div>
</nav>

<div class="container">
